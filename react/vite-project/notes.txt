-

-browser only understands before ES6
'babeljs' - it is a transpiler which converts advanced js into previous js

== react uses a module bundle called as 'roll up'


'npm create vite@latest' - to install react project
'npm install' - to install node modules


-- Old way 
React.createElemet(tag, attribute, content)
React.creataelement("h1",null,"this is h2")


- jsx - javascript and xml
    xml - extensible markup language 
        - to create custom tags
        - to pass the data from one tag to another tag
        
in app.jsx -
    - javascript code should be above the return
    - html should be  inside the return

Rules of JSX
1) jsx must have one parent element
2) parenthisis is required while returning multiline statements
3) all tags must be closed 
4) attributes should be in camel case
5) - instead of 'class'  you should use 'className'   and   ' for'  = 'HtmlFor'

Expression - to access content of javascript in html we use expression ({})

- undefined and objects will not be printed in the ui

= jsx will not allow  statements and loops
= jsx will only allow map,filter,reduce and ternary operator


FRAGMENTS- allow developers to group multiple elements without adding extra nodes to the DOM.

- COMPONENTS- 
 -1) class based component 
 -2) function based component
Rules of components
1) component name should always start with 'capital letter'
2) 


-PROPS - sending data from one component to another component is known as Props(properties).

-HOOKS - all the functionalities in classbased component if we want to use it in function based dcomponent we use 'hooks'.
-whenever the state changes the component refreshes

1) useState - 

- 'conditional rendering' - 

1. if-else
2. ternary operator
3.short circuit

npm install react-icons --- to install react icons


- Form handling
- 2 ways forr handling form
1) controlled way(useState)
2) uncontrolled way(useRef)


1- useRef - used to directly manipulate real DOM
== 'synthetic event' - it is a cross-browser event that allows your native browser to adapt the events

"Postin Data" - 
"npm install json-server"  - command to install json server

1}--npm init -y    == to install package.json
2}--npx install json-server  == to install json server -you can see it on dependencies
npm(node package manager), npx(node package execute)bb

3}npx json-server --watch ./backend/db.json --port 4200
